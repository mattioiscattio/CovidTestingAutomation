import tkinter as tk

window = tk.Tk() #sets the window for tk
widget = tk.Label(text="text widget go BRRRRR", foreground="#FFFFFF", background="#000000", )#defines a label widget with specific text and colours using hexidecimal.
widget.pack()
window.mainloop()#opens the window
