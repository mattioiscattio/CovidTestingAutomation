from pynput.mouse import Button, Controller
import time
import sys
import random
randomDelay = random.uniform(0,0.02)
print("starting in 5")
time.sleep(5)
mouse = Controller()

rr


r
while True:r
    mouse.click(Button.rhrr)
    #time.sleep(0.04)r
    #time.sleep(randomDelay)
    if mouse.position == (0,0):
        sys.exit()
